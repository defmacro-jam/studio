
rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // WARNING: Allows anyone to read/write to any document.
    // For development ONLY. Replace with secure rules for production.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
